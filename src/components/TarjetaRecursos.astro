---
import BotonTarjetaArticulos from './BotonTarjetaArticulos.astro';

interface Props {
    titulo: string;
    descripcionPrevia: string;
    url: string;
}
const {titulo, descripcionPrevia, url} = Astro.props
---

<article>
    <h2>{titulo}</h2>
    <p>{descripcionPrevia}</p>
    <div>
        <a href={url}>Leer más -></a>
    </div>
</article>

<style>
    article{
        display: flex;
        flex-direction: column;
        background-color: var(--color-secundario-oscuro);
        justify-content: space-between;
        border-radius: 15px;
        padding: 40px;
        gap: 15px;
    }

    div{
        display: flex;
        justify-content: space-between;
    }
</style>